// Import jest-dom for DOM testing utilities
import '@testing-library/jest-dom';

// Set up global mocks if needed
// For example, if you need to mock fetch:
// global.fetch = vi.fn();

// You can also set up other global configurations here
// For example, suppress console errors during tests:
// console.error = vi.fn();

// This file runs before each test file